shell bash

defhstatus "^Et (screen ^E) | $USER@^EH"
defscrollback 1000
defutf8 on

# erase background with current bg color
defbce "on"

termcapinfo xterm* 'hs:ts=\E]0;:fs=\007:ds=\E]0;\007'
# tell screen how to set colors. AB = background, AF=foreground
# the first two are for other terminal emulators, just in case.
termcapinfo xterm "Co#256:AB=\E[48;5;%dm:AF=\E[38;5;%dm"
termcapinfo xterm-color "Co#256:AB=\E[48;5;%dm:AF=\E[38;5;%dm"
termcapinfo xterm-256color "Co#256:AB=\E[48;5;%dm:AF=\E[38;5;%dm"
term xterm-256color

#change the hardstatus settings to give an window list at the bottom of the
#screen, with the time and date and with the current window highlighted
#hardstatus alwayslastline
#hardstatus string '%{= kG}%-Lw%{= kW}%50> %n%f* %t%{= kG}%+Lw%< %{= kG}%-=%c:%s%{-}'
#hardstatus string '%{= kG}[ %{G}%H %{g}][%= %{= kw}%?%-Lw%?%{r}(%{W}%n*%f%t%?(%u)%?%{r})%{w}%?%+Lw%?%?%= %{g}][%{B} %m-%d %{W}%c %{g}]'

#bindkey -k k3 prev  # change window with ctrl-left
#bindkey -k k4 next  # change window with ctrl-right
bindkey -k k2 resize -6  # change window with ctrl-right
bindkey -k k3 resize max  # change window with ctrl-right

altscreen on

# allow bold colors ¨C necessary for some reason
attrcolor b ".I"

vbell off
startup_message off

# files
hardcopydir /local/guangmi1/tmp
logfile /local/guangmi1/tmp/screenlog.txt
bufferfile /local/guangmi1/tmp/screen_buffer.txt

# start the windows
chdir /local/guangmi1/cpeg_sf3g_lpop/android
screen -t flycm_android 0
chdir /local/guangmi1/cpeg_sf3g_lpop/linux-3.10
screen -t flycm_kernel 1
chdir
screen -t home 2
screen -t CONSOLE 3
chdir /local/guangmi1/others
screen -t others 4
chdir /local/guangmi1/WML
screen -t main_line 5
chdir
screen -t lynx 6
chdir
#screen -t BUILD_1 5
#screen -t BUILD_2 6
#screen -t BUILD_3 7
#screen -t BUILD_4 8
#select 5
#split
#split -v
#focus down
#select 6
#focus down
#split -v
#select 7
#focus down
#select 8
select 0
split
focus down
select 3
focus down
resize max
